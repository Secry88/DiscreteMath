<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:vm="using:DiscreteMath.ViewModels"
             x:DataType="vm:TheoryCategoryViewModel"
             xmlns:controls="using:DiscreteMath.CustomControls"
             x:Class="DiscreteMath.Views.TheoryCategoryView">
    <Grid Background="#F5F5F7">
        <Grid RowDefinitions="Auto,Auto,*" Margin="40,22,40,32">
            <Button Grid.Row="0"
                    HorizontalAlignment="Left"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding NavigateBackCommand}">
                <TextBlock Text="← На главную"
                           FontSize="22"
                           Foreground="#6E6785"/>
            </Button>

            <Grid Grid.Row="1"
                  Margin="0,18,0,28"
                  ColumnDefinitions="Auto,*"
                  ColumnSpacing="14">
                <Border Grid.Column="0"
                        Width="56"
                        Height="56"
                        Background="#7C3AED"
                        CornerRadius="16"
                        VerticalAlignment="Center">
                    <TextBlock Text="📖"
                               FontSize="26"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                    <TextBlock Text="Теория"
                               FontSize="42"
                               FontWeight="SemiBold"
                               Foreground="#1A1625"/>
                    <TextBlock Text="Выберите категорию для изучения"
                               FontSize="17"
                               Foreground="#6E6785"/>
                </StackPanel>
            </Grid>

            <ItemsControl Grid.Row="2"
                          HorizontalAlignment="Center"
                          ItemsSource="{Binding Categories}">
                <ItemsControl.Styles>
                    <Style Selector="ContentPresenter">
                        <Setter Property="Margin" Value="14"/>
                    </Style>
                </ItemsControl.Styles>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:TheoryCategoryCardControl
                            Title="{Binding Title}"
                            Description="{Binding Description}"
                            Command="{Binding $parent[UserControl].DataContext.OpenCategoryCommand}"
                            CommandParameter="{Binding Id}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
